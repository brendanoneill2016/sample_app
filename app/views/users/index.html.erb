<% provide(:title, 'All users') %>
<h1>Banter Central</h1>

<%= will_paginate %>


<ul class="users">
  <% @users.each do |user| %>
 	<%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>

<%= link_to("Generate Report", users_path(format: :csv), class: "btn btn-success",) %>